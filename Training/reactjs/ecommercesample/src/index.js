import React from 'react';
import ReactDOM from 'react-dom/client'; 
import CSSSample from './HorizontalList'; 
 
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CSSSample />
  </React.StrictMode>
);
