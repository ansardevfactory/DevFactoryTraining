import React from 'react';
import ReactDOM from 'react-dom/client';  
import Sample from './SelectSample'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Sample />
  </React.StrictMode>
); 